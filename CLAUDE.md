# LanSync

Agent notes: **[docs/CLAUDE.md](docs/CLAUDE.md)** (Go module/Dependabot hygiene, ServiceMonitor deployment).

Cursor rules live under `.cursor/rules/` (committed). Local Claude session state under `.claude/` is gitignored.
